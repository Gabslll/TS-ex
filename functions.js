"use strict";
function multiplicar(a, b) {
    return a * b;
}
console.log(multiplicar(20, 15));
function saudacao(nome) {
    nome = "Olá" + "Gabriel";
    if (nome == "Gabriel" || "Olá") {
        console.log(`${nome} seja bem vindo.`);
    }
    else {
        console.log(`Nome não corresponde com o cadastro`);
    }
}
